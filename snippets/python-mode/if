# -*- mode: snippet -*-
# name: wrap with if
# key: if
# expand-env: ((yas-indent-line 'fixed) (col (current-indentation)))
# --
`(make-string col ?\s)`if ${1:condition}:
`(yas-body (+ col 4))`${2:}
$0
