;;; secrets.el -*- lexical-binding: t; -*-
;;
;; Copy this file to secrets.el and fill in machine-local values.
;; secrets.el is git-ignored.
;;
;; Examples of what tends to live here:
;;
;;   (setenv "OPENAI_API_KEY"     "sk-...")
;;   (setenv "ANTHROPIC_API_KEY"  "sk-ant-...")
;;   (setq auth-sources '("~/.authinfo.gpg"))
;;
